{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/build/App/App${env:EXE_SUFFIX}${env:TARGET_SUFFIX}",
      "args": [],
      "preLaunchTask": "build",
      "cwd": "${workspaceFolder}",
      "windows": {
        "program": "${workspaceFolder}/build/App/Debug/App${env:EXE_SUFFIX}${env:TARGET_SUFFIX}"
      }
    }
  ]
}
